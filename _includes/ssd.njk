<!doctype html>
<html lang="en">
<head>
     <meta charset="UTF-8">
     <meta name="viewport"
           content="width=device-width,
           user-scalable=no,
           initial-scale=1.0,
           maximum-scale=1.0,
           minimum-scale=1.0">
     <meta http-equiv="X-UA-Compatible"
           content="ie=edge">
     <title>{{ title }}</title>
     <link rel="stylesheet" href="css/ouch.css">
</head>
<body>
 {% include "layouts/top.html" %}

 <h1>{{ title }}</h1>

 <h2>{{ param }}</h2>

    {{ content | safe }}

<div class="flex">
{% for file in image_metadata -%}
         <figure><img src="{{ file.filepath }}" alt="{{ file.filename }}"/>
             <figcaption>{{ file.filename }}</figcaption>
         </figure>
         {% endfor -%}
</div>

maybe theres a way to have it read files with only the ssd type tag?

 {% include  "layouts/footer.html" %}

</body>
</html>